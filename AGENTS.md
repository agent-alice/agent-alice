# Alice 协作规范

Alice 使用 Codex CLI / App Server 的公开接口。不要修改或复制 Codex 核心，不重复实现模型循环或上下文压缩。迁移仍在进行；组件测试通过不等于完整切换或自主能力通过。

## 开始工作

- 先查现有 issue 和 PR，避免重复任务；在 issue 写清范围、依赖、验收条件和当前未完成项。
- 每项可独立验收的工作使用单独分支及 worktree，声明文件所有权。共享接口先协调，不覆盖其他协作者的改动。
- 在既有授权范围内推进；发现范围或依赖变化时及时更新 issue。敏感操作和外部发送遵循当前任务的明确授权。

## 实现与证据

- Git 只保存代码、合成 fixtures 和项目文档。身份、记忆、私密 prompt、原始日志、凭据及本机部署路径留在仓库外；报告只放脱敏统计和必要 hash。
- 数据格式、持久 ID 或路径变化要有显式迁移与回退方案；保留新记录，不用旧快照覆盖运行数据。
- 运行与改动相关的实际测试。区分单元／模拟、真实二进制受控运行、安装产物和真实模型证据；记录 commit、命令、数量、退出状态与适用范围。
- 失败、跳过、超时、空测试集或模型自述不算通过。不能添加空成功 CI，也不能把旧产物的报告套到新产物。

## 提交与交接

- PR 关联 issue，说明最终行为、证据、风险及尚未完成项；仍待集成时保持 Draft。
- 合入、部署与最终切换由负责集成的人执行。只激活经过验证的候选，回退应保留新增数据并检查 schema 兼容性。
- 完成后交接具体结果与证据，不仅报告“已完成”。本规范不意味着当前已经配置 CI、必需检查或完成生产切换。
